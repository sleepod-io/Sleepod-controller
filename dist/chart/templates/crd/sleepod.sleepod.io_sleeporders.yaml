{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.18.0
  name: sleeporders.sleepod.sleepod.io
spec:
  group: sleepod.sleepod.io
  names:
    kind: SleepOrder
    listKind: SleepOrderList
    plural: sleeporders
    singular: sleeporder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.currentState
      name: Current_State
      type: string
    - jsonPath: .status.originalReplicas
      name: Original_Replicas
      type: integer
    - jsonPath: .status.lastTransitionTime
      name: Last_Transition_Time
      type: date
    - jsonPath: .status.nextOperationTime
      name: Next_Operation_Time
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SleepOrder is the Schema for the sleeporders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SleepOrder
            properties:
              sleepAt:
                pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                type: string
              targetRef:
                properties:
                  apiVersion:
                    default: apps
                    type: string
                  kind:
                    enum:
                    - Deployment
                    - StatefulSet
                    type: string
                  name:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              timezone:
                default: UTC
                type: string
              wakeAt:
                pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                type: string
            required:
            - sleepAt
            - targetRef
            - wakeAt
            type: object
          status:
            description: status defines the observed state of SleepOrder
            properties:
              currentState:
                enum:
                - Sleeping
                - Awake
                - Error TargetNotFound
                - Error
                type: string
              lastTransitionTime:
                format: date-time
                type: string
              nextOperationTime:
                format: date-time
                type: string
              originalReplicas:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
